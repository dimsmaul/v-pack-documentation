---
sidebar_position: 2
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';
export const packageList = "v-pack twrnc dayjs zod react-hook-form @hookform/resolvers"


# Installation

## Requirements

- Node.js >= 16
- React Native >= 0.70
- React >= 18

## Init React Native

### Expo

```bash
npx create-expo-app my-app
cd my-app
```

### React Native CLI
```bash
npx react-native init MyApp
cd MyApp
```

Install Package
<Tabs groupId="package-manager">
  <TabItem value="npm" label="npm">
    <CodeBlock language="bash">npm install {packageList}</CodeBlock>
  </TabItem> 
  <TabItem value="yarn" label="yarn">
    <CodeBlock language="bash">yarn add {packageList}</CodeBlock>
  </TabItem> 
  <TabItem value="pnpm" label="pnpm"> 
    <CodeBlock language="bash">pnpm add {packageList}</CodeBlock>
  </TabItem> 
</Tabs> 

Initialize V-Pack
```bash
npx v-pack init
```
Creates ```tailwind.config.js``` in your project root.

## Setup Provider

```tsx
// App.tsx
import { VPackProvider } from 'v-pack';
import theme from './tailwind.config.js';

export default function App() {
  return (
    <VPackProvider theme={theme}>
      <YourApp />
    </VPackProvider>
  );
}
```